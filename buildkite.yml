steps:
  - name: ':pipeline: Test'
    command: 'make test'
    agents:
      queue: 'macos'

steps:
  - name: ':pipeline: Build Examples'
    command: 'make build-example'
    agents:
      queue: 'macos'

