import SwiftUI
import TinkLink

struct ContentView: View {
    var body: some View {
        NavigationView {
            CredentialsView()
        }
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
